"""
scripts — Setup, build, and verification scripts.

    - setup_ubuntu.sh: Environment setup for Ubuntu + 8 GB VRAM GPU (GPU check, venv, deps)
    - build_index.py:  One-time ontology index builder (~15–20 min first run)
    - smoke_test.py:   End-to-end verification of all components before using the UI
"""
